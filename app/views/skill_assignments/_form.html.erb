<%= form_for(@skill_assignment) do |f| %>
  <% if @skill_assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@skill_assignment.errors.count, "error") %> prohibited this skill_assignment from being saved:</h2>

      <ul>
      <% @skill_assignment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :employee_id %><br>
    <%= f.number_field :employee_id %>
  </div>
  <div class="field">
    <%= f.label :skill_id %><br>
    <%= f.number_field :skill_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
