<%= form_for(@shift_assignment_status) do |f| %>
  <% if @shift_assignment_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shift_assignment_status.errors.count, "error") %> prohibited this shift_assignment_status from being saved:</h2>

      <ul>
      <% @shift_assignment_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
